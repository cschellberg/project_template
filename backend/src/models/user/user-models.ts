

export interface User {
    firstName: string;
    lastName: string;
    email: string;
    id?: string;
}

export interface UserInput {
    firstName: string;
    lastName: string;
    email: string;
}